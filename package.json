{
    "name": "@galaxyproject/olite",
    "version": "0.0.0",
    "type": "module",
    "license": "MIT",
    "files": [
        "static"
    ],
    "scripts": {
        "dev": "npm run build && vite",
        "build": "npm run build:pyodide && npm run build:olite && vite build",
        "build:pyodide": "node pyodide.install.js",
        "build:olite": "cd brain && rm -rf build dist olite.egg-info && python -m build --wheel && cp dist/olite-*.whl ../static/pyodide/",
        "preview": "vite preview",
        "prettier": "prettier --config ./prettier.config.js --write './*.{js,ts,json}' 'src/**/*.{js,ts,json,css}'",
        "test": "npm run test:unit && npm run test:python",
        "test:python": "cd brain && python -m pytest tests/ -v --import-mode=importlib",
        "test:ui": "vitest --ui",
        "test:unit": "vitest run",
        "test:watch": "vitest --watch"
    },
    "dependencies": {
        "vega-embed": "^7.1.0"
    },
    "devDependencies": {
        "@types/dompurify": "^3.0.5",
        "@types/node": "^22.9.0",
        "dompurify": "^3.4.1",
        "galaxy-charts-xml-parser": "^1.0.3",
        "happy-dom": "^20.0.11",
        "marked": "^18.0.0",
        "prettier": "^3.7.4",
        "pyodide": "^0.29.0",
        "typescript": "^5.9.3",
        "vite": "^7.3.0",
        "vite-plugin-static-copy": "^3.1.4",
        "vitest": "^4.0.16"
    }
}
