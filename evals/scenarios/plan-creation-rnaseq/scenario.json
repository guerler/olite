{
  "name": "plan creation: bulk RNA-seq differential expression",
  "description": "Two turns: the user asks for a DE plan, then confirms assumptions. SUCCESS = a structurally valid `## Plan X: <title> [routing]` block appears in chat, since olite's gate draws the draft in chat and only writes an approved plan to the record. Biological correctness beyond naming a plausible aligner/DE tool is out of scope. Inputs and thresholds aligned byte-for-byte with loom's scenario of the same name (2026-08-19) so both suites grade the same task; `routingIn` stays olite-specific because olite has no local execution and that dimension is excluded from the comparison.",
  "tier": 2,
  "inputs": [
    "I have paired-end RNA-seq FASTQ files from two conditions (treated vs control, 4 replicates each). Help me draft a plan to identify differentially expressed genes. Don't execute anything yet.",
    "Yes, those assumptions all look right. Use hg38 as the reference. Go ahead and draft the formal plan now."
  ],
  "timeoutMs": 420000,
  "assertions": {
    "events": {
      "mustInclude": [
        "turn_start",
        "turn_end"
      ]
    },
    "plan": {
      "exists": true,
      "routingIn": [
        "galaxy",
        "remote"
      ],
      "minPendingSteps": 4,
      "eachStepHasDescription": true,
      "mentionsOneOf": [
        "HISAT2",
        "STAR",
        "salmon",
        "kallisto",
        "DESeq2",
        "edgeR",
        "limma",
        "featureCounts"
      ]
    },
    "behavior": {
      "doesNotExecute": true
    }
  }
}
