[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "olite"
version = "0.0.0"
description = "olite agent brain (client-only Galaxy co-scientist)"
dependencies = ["numpy", "pandas"]
readme = "README.md"
requires-python = ">=3.7"
license = "MIT"
authors = [{ name = "Galaxy Team" }]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent"
]

[project.optional-dependencies]
dev = ["black", "ruff", "mypy", "pytest", "pytest-asyncio", "aiohttp"]

[tool.black]
line-length = 120
target-version = ["py37"]

[tool.ruff]
select = ["E","F","I"]
ignore = []
line-length = 120

[tool.mypy]
python_version = "3.7"
strict = false

[tool.setuptools.packages.find]
include = ["olite*"]

[tool.setuptools.package-data]
"olite.registry" = ["processes/*.yml", "skills/*.md"]
